const jwt = require('jsonwebtoken');   

const fetch_user = (res, req, next)=>{       //this is middleware function which takes parameter next to execute the next func. 
    
    next()
}
module.exports = fetch_user;